{
  "name": "create-next-template",
  "version": "2.0.0",
  "description": "Scaffold a new Next.js project from CSR or SSR enterprise templates",
  "type": "module",
  "bin": {
    "create-next-template": "./dist/bin/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx bin/index.ts",
    "link-cli": "npm run build && npm link"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "fs-extra": "^11.3.0",
    "inquirer": "^12.6.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20",
    "tsx": "^4.19.4",
    "typescript": "^5"
  }
}
